[pytest]
addopts = --maxfail=1 --disable-warnings --cov=.
markers =
    quick_regression: user story tests (fast subset)
    full_regression: comprehensive test suite
    negative: negative validation tests
